<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.logic.taxi.mapper.TaxiInfoMapper">
  <select
    id="findList"
    resultType="com.logic.taxi.bean.TaxiInfoBean">
    select
    info.id,
    sdi.itemName as serviceType,
    info.price,
    info.subscriptionDate,
    info.description,
    info.createTime
    from taxi.taxi_info info
    left join taxi.sys_dictionary_item sdi on sdi.itemValue=info.serviceType
    where info.type=#{type}
    and sdi.typeID=1
    order by createTime desc
    limit 30
  </select>
</mapper>
